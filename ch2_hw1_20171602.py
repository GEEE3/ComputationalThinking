# by GEEE3, March 5, 2021
# 3.14를 변수에 할당한 후, 다음 순서대로 실행하는 script를 작성할 것

# 1. 문자열로 변환 후, 값과 데이터 형을 출력
a = 3.14
a = str(a)
print(a, type(a))

# 2. 변환된 문자열을 정수로 변환 후, 값과 데이터 형을 출력
a = float(a)
a = int(a)
print(a, type(a))

# 3. 변환된 정수를 실수로 변환 후, 값과 데이터 형을 출력
a = float(a)
print(a, type(a))